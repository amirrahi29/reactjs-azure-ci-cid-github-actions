# Dev — git branch `dev` → CI namespace `dev` → image tag `dev-<sha>` → yeh file: helm/values-dev.yaml
# Push `dev` par: .github/workflows/chat-app.yml → ACR → helm upgrade -f helm/values-dev.yaml

replicaCount: 1

resources:
  limits:
    cpu: 100m
    memory: 96Mi
  requests:
    cpu: 25m
    memory: 48Mi

podAnnotations:
  environment: dev

service:
  type: LoadBalancer

ingress:
  enabled: false
  className: nginx
  hosts:
    - host: app-dev.mergeai.in
      paths:
        - path: /
          pathType: Prefix

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
